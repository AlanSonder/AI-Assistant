你是一个资深前端工程师，正在为本“桌面 AI 助手”项目开发第一阶段 UI。

技术栈要求：

- Vue 3（script setup）
- TypeScript
- Vite
- Element Plus
- Axios
- 使用 Pinia（但当前页面可以不复杂使用）

目标：\
实现一个“翻译控制台页面”，用于调用后端翻译接口。

***

请生成一个完整的 Vue 页面组件（.vue），要求如下：

一、页面结构（UI）\
使用 Element Plus 实现，布局简洁现代：

1. 顶部：

- 标题：AI 翻译助手
- 显示模型状态（固定写：LM Studio 已连接）

1. 输入区：

- 多行输入框（textarea）
- placeholder：请输入要翻译的内容
- 支持自动高度

1. 语言选择区：

- from：源语言（默认 auto）
- to：目标语言（默认 en）
- 使用 el-select

语言选项：

- auto（自动检测）
- zh（中文）
- en（英文）
- ja（日文）

1. 操作按钮：

- “翻译”按钮（primary）
- “清空”按钮
- 翻译时按钮 loading

1. 输出区：

- 卡片样式
- 显示翻译结果
- 支持复制按钮

***

二、功能要求

1. 点击“翻译”：

- 调用 后端接口,支持文本,图片,音频输入,分别调用对应的后端接口
- 使用 axios
- 显示 loading 状态
- 成功后展示结果
- 失败时弹出 error 提示（ElMessage）

1. 输入校验：

- 为空时禁止翻译
- 给出提示

1. 清空按钮：

- 清空输入和结果

1. 复制功能：

- 点击复制结果到剪贴板
- 成功提示

***

三、代码要求

- 使用
- 使用 ref/reactive 管理状态
- axios 封装为简单函数（可写在当前文件中）
- 代码结构清晰，有适当注释
- 不要过度拆组件（单文件即可）

***

四、样式要求

- 使用 scoped style
- 页面居中（最大宽度 800px）
- 留白舒适（padding）
- 卡片阴影
- 不要复杂 UI，但要“像产品”

***

五、额外要求（重要）

- 代码必须可直接运行
- 不要省略 import
- 不要写伪代码
- 输出完整代码

***

现在开始生成代码。
